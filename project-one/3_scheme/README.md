# Empty C# Project

This is a minimal C# console project created in this folder.

How to build and run (PowerShell):

```powershell
cd e:\NUWGP\dis-math\EmptyCsProject
dotnet build
dotnet run
```

If `dotnet` is not installed, install from https://dotnet.microsoft.com/
